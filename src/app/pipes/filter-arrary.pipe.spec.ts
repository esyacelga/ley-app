import { FilterArraryPipe } from './filter-arrary.pipe';

describe('FilterArraryPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterArraryPipe();
    expect(pipe).toBeTruthy();
  });
});
